import { GlobalConfig } from '@ray-js/types';

const config = {
  window: {
    backgroundColor: '#f7f3ec',
    navigationBarTitleText: 'HSHH',
    navigationBarBackgroundColor: '#f7f3ec',
    navigationBarTextStyle: 'black',
  },
} as unknown as GlobalConfig;

export default config;
